#!/usr/bin/env python3

from setuptools import setup, find_packages

setup(
    name="phone-camera-upload",
    version="0.1.0",
    packages=find_packages(),
    scripts=["scripts/phone-camera-upload"],
)
